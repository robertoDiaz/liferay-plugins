YUI.add('aui-image-filter-grayscale', function (A, NAME) {

var ImageFilterGrayscale = function (bitmapData, width, height) {
	console.log(bitmapData);
	console.log(width);
	console.log(height);
}

A.ImageFilterGrayscale = ImageFilterGrayscale;

}, '2.0.0');
